fastapi==0.110.0
uvicorn==0.28.0
streamlit==1.32.0
chromadb==0.4.24
pymupdf==1.23.26
python-multipart==0.0.9
requests==2.31.0
pydantic==2.6.4
pandas==2.2.1

# Opsiyonel (Faz 4 "iyi kat" parser — alt-bölüm çözünürlüğü). Ağırdır (torch).
# Kurulmazsa sistem PyMuPDF sezgisel parser'a düşer (fail-safe).
#   pip install docling
